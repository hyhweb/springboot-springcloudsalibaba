server:
  port: 8080
spring:
  application:
    name: gateway-service
  cloud:
    nacos:
      config:
        server-addr: 192.168.226.138:8848
        file-extension: yml
      discovery:
        server-addr: 192.168.226.138:8848
    gateway:
      discovery:
        locator:
          enabled: true

# 配置日志级别，方别调试
logging:
  level:
    org.springframework.cloud.gateway: debug
